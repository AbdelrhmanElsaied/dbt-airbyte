dbt_airbyte_test:
  target: dev
  outputs:
    dev:
      type: mysql
      server: 194.163.140.33
      port: 3305
      database: mariadb
      schema: mariadb
      username: mariadb
      password: mariadb
      driver: MySQL ODBC 8.0 ANSI Driver
    prod:
      type: mysql
      server: [server/host]
      port: [port]  # optional
      database: [schema] # optional, should be same as schema
      schema: [schema]
      username: [username]
      password: [password]
      driver: MySQL ODBC 8.0 ANSI Driver
